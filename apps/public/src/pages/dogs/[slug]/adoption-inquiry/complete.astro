---
// SCR-50 — PRD-04 §3-1 (guarded)
import { env } from "cloudflare:workers";
import Layout from "../../../../layouts/Layout.astro";
import CompletionPanel from "$lib/components/completion-panel.astro";
import { getSession } from "$lib/server/auth/session";
import { createDb } from "@app/schema/client";

const db = createDb(env.DB);
const session = await getSession(Astro.cookies, db);
if (!session) return Astro.redirect("/auth/login", 302);
---

<Layout title="相談を送信しました | 保護犬おさんぽマッチング">
  <main id="main-content" class="mx-auto max-w-2xl px-4 py-16">
    <CompletionPanel title="相談を送信しました" message="保護団体から直接ご連絡があります。やり取りの状況はマイページで確認できます。" actionHref="/mypage/adoption-inquiries" actionLabel="相談の一覧へ" />
  </main>
</Layout>
